- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.Xmodmap: Xmodmap
    ~/.Xinitrc: Xinitrc
    ~/.i3: i3
    ~/.i3status.conf: i3status.conf

- shell:
    - [get submodule update --init --recursive, Installing submodules]
